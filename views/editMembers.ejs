<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Edit Member</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/MembersHomepage">Back</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Member</h2></div>
        <br>
        <div class="text-center">
          <% if (members) { %> 
            <form action="/editMembers/<%= members.id %>" method="POST">
                <label for="student_id">Student ID:</label><br>
                <input type="text" id="student_id" name="student_id" value="<%= members.student_id %>" required><br><br>
            
                <label for="ig_id">IG ID:</label><br>
                <input type="text" id="ig_id" name="ig_id" value="<%= members.ig_id %>" required><br><br>
            
                <label for="role">Role:</label><br>
                <input type="text" id="role" name="role" value="<%= members.role %>" required><br><br>
                
                <label for="joined_date">Joined Date:</label><br>
                <input type="text" id="joined_date" name="joined_date" value="<%= members.joined_date %>" required><br><br>
            
                <button type="submit" class="btn btn-primary">Update Member</button>
            </form>
            
          <% } else { %>
              <p>No Member found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
