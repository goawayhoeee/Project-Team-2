<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>school list app</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/schoolHomepage">Back</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addschool">Add new school</a>
              </li> 
            </ul>
            <form class="d-flex" action="/Schoolsearch" method="GET">
            <input class="form-control me-2" type="search" name="query" placeholder="Search schools..." aria-label="Search">
            <button class="btn btn-outline-light" type="submit">Search</button>
          </form>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update school</h2></div>
        <br>
        <div class="text-center">
          <% if (school) { %> 
            <form action="/editschool/<%= school.schoolid %>" method="POST">
                <label for="name">school name:</label><br>
                <input type="text" id="name" name="name" value = "<%= school.name %>" required><br><br>

                <label for="address">Address:</label><br>
                <input type="text" id="address" name="address" value = "<%= school.address %>" required><br><br>

                <label for="contact">contact:</label><br>
                <input type="text" id="contact" name="contact" value = "<%= school.contact %>" required><br><br>

                <label for="image">current  Image:</label><br>
                <input type = "text" name ="currentImage" value = "<%= school.school_logo %>" readonly><br>
                <img src = "/images/<%= school.school_logo %>" width="20%"><br><br>

                New Image <br> <input type="file" id = "image" accept="image/*" value="<%= school.school_logo %>"><br><br>

                <button type="submit">Update school</button>
            </form>
          <% } else { %>
              <p>No school found.</p>
          <% } %>
          <a href ="/schoolHomepage">Back</a>
        </div>
    </div>
</body>
</html>
